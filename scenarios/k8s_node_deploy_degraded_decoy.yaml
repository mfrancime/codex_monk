# k8s_node_deploy_degraded_decoy — NODES FRONT (k8s_api / K). DECOY.
# Counter-move vs the champion's degraded cheat (Kd2>→Cx detects "nodes down"
# via deployments.degraded). Here a bad rollout leaves 5 Deployments degraded
# but EVERY node is Ready (not_ready=0) — that is a scheduler/app problem, not a
# node problem. The nodes front must stay silent. Forces gating on not_ready.
name: k8s_node_deploy_degraded_decoy
domain: k8s_api
ticks_total: 150
phases:
  - name: deploys_degraded_nodes_ready
    range: [0, 149]
    frame:
      k8s.api.healthy: 1
      k8s.api.latency_ms: 28
      k8s.nodes.total: 5
      k8s.nodes.not_ready: 0
      k8s.events.warnings_60s: 10
      k8s.deployments.degraded: 5
      k8s.api.auth_present: 1
    truth: null
